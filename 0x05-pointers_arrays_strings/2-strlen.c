#include <stdio.h>
#include "main.h"

/**
 * _strlen - function for counting the lenght of string
 * @s: a pointer for counting the string lenght
 * Author: AkutaDavid
 * Return: int i
 */

int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		!++;
	}
	return (i);
}
