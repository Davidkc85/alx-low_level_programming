#include "main.h"

/**
 * print_most_numbers - prints number
 */

void print_most_numbers(void)
{
	int;
	for (i = 48; i < 58; i++)
	{
		if (i != 50 && i != 52)
		{
			_putcha(i);
		}
	}
	_putchar('\n');
}
